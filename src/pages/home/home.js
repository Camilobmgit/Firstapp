import Button from 'react-bootstrap/Button';
const Home = (props) => {

    return (
        <div className="container text-primary" >
            HOME
            <Button />
        </div>
    );
}

export default Home;